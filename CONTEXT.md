# CONTEXT.md

本仓库的领域词汇表。它只定义语言，不放实现细节，也不当规格用。

## 智能体

对话智能体：管理员配置的专属 AI 助手，拥有自己的系统提示词、欢迎语、绑定的大模型与知识库。
报表与文档中单独出现「智能体」时，一律指它。

> 注意区分：爬虫的采集日志里也有一个名为 `agent` 的字段，取值是 PLANNER / EXTRACTOR / CRAWLER 等
> **流水线阶段**，与对话智能体不是同一个概念，两者不可混为一谈。

## 用量流水

一次模型调用留下的一条计量记录。它只增不改，是账单的事实来源。
一条用量流水归属到发生这次调用的智能体、用户与会话，并记录该次调用消耗的 token。

## 估算用量

模型服务没有返回用量时，按文本长度推算出来的 token 数。它只是兜底值，不是事实。
估算用量不进入任何统计数字，只保留在流水里备查。

## 问答次数

用户发起的一次提问。一次提问只算一次，无论它在内部触发了几次模型调用。
命中「知识库中未找到资料」而根本没有调用模型的提问不算。

## 模型调用次数

系统向模型服务发出的调用次数。一次提问背后往往不止一次调用：为检索改写问题、
压缩较早的对话、查询数据库、执行工具，都会各自产生调用。
因此同一范围内，模型调用次数通常大于问答次数；两者不可混用。

## 用量统计

管理员查看模型消耗的报表：在某个时间范围与某个智能体作用域内，汇总用量流水得出的数字。
